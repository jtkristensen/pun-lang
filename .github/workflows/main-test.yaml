name: tests
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  stack-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: freckle/stack-action@v4
